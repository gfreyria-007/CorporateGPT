# AI Memory - Corporate GPT & Techie

## Project Overview

- **Product**: Corporate GPT (enterprise AI platform) + Techie (kids educational AI workspace)
- **Owner**: gfreyria@gmail.com (Gabriel Freyria)
- **Repo**: github.com/gfreyria-007/CorporateGPT
- **Stack**: React + TypeScript + Vite (frontend), Express + tsx (backend), Firebase (Auth + Firestore), OpenRouter (AI routing)
- **Domain**: catalizia.com

## Infrastructure

| Component | Value |
|---|---|
| GCP Project | `corporategptv2` |
| Cloud Run Service | `corporate-gpt-prod` |
| Region | `us-central1` (ONLY, northamerica-south1 deleted) |
| Cloud Build Trigger | Push to `main` branch, uses `cloudbuild.yaml` |
| Firebase Project | `corporategptv2` |
| Health Check | `/api/health` returns JSON with version, security status, timestamp |
| Build Version | `5.0.3` |

## Architecture Decisions

### AI Engine Routing
- All AI requests route through OpenRouter via backend proxy (`/api/chat`, `/api/techie`, `/api/fallback`)
- "Immortal Architecture": multi-model failover with prioritized model chains
- Default model: `openrouter/auto` (auto-routes to best available)
- Both Corporate GPT and Techie share the same model list from `/api/models`
- User can select specific models via `ModelSelector` component
- **Techie standardized**: All client-side SDKs (`GoogleGenAI`) and hardcoded keys removed. Techie routing is strictly proxy-only via `/api/techie`.

### Techie Workspace
- Techie is a child workspace inside Corporate GPT, rendered via `TechieWorkspace.tsx` bridge
- Entry point: `src/Techie/src/TechieMain.tsx`
- Service layer: `src/Techie/src/services/geminiService.ts` (now 100% proxy-based)
- Techie uses the same backend proxy `/api/techie` which inherits the Immortal Architecture failover
- `getChatResponse` accepts optional `selectedModel` parameter (defaults to `openrouter/auto`)
- **UI Standardized**: `ModelSelector` integrated into `ChatInput` area, replacing the "Cosas Pro" legacy label.
- **Mascot**: Local asset `/techie-mascot.png` replaces broken hardcoded external link.
- MonthlyQuotaBanner removed from Techie (not kid-appropriate)
- Responsive Layout optimized for `h-screen overflow-hidden`.


### Responsive Layout (Techie)
- Root container: `h-screen overflow-hidden` (not min-h-screen)
- Chat area: `flex-1 flex flex-col min-h-0` with `overflow-y-auto` inner div
- Header/footer: wrapped in `shrink-0`
- Top controls (grade + model selector): single compact row with `flex-wrap`

### Security
- Helmet middleware active
- Rate limiting active
- CSP enforced
- TLS enabled
- Admin: gfreyria@gmail.com only

## Key File Paths

| Purpose | Path |
|---|---|
| Main App Shell | `src/App.tsx` |
| Techie Entry | `src/Techie/src/TechieMain.tsx` |
| Techie Service | `src/Techie/src/services/geminiService.ts` |
| Backend Server | `server.ts` |
| Model Selector | `src/components/ModelSelector.tsx` |
| Image Model Selector | `src/components/ImageModelSelector.tsx` |
| Techie Workspace Bridge | `src/components/TechieWorkspace.tsx` |
| Cloud Build Config | `cloudbuild.yaml` |
| Dockerfile | `Dockerfile` |
| Firebase Config | `firebase.json` |

## User Preferences

- No billing/quota UI in Techie (kids don't need corporate info)
- Model selector must be compact, not take up chat space
- UI must be super responsive, no overlapping on large screens
- Only us-central1 region (no multi-region)
- Admin access: gfreyria@gmail.com only
- Techie should share all CorporateGPT resources and APIs

## Memory Log

### Session: 2026-05-06

#### Technical Decisions
- Unified Techie AI engine with CorporateGPT's OpenRouter-based auto-router
- Added `ModelSelector` to Techie workspace (compact, inline with grade selector)
- Added `ImageModelSelector` to Techie workspace for image generation parity
- Removed `MonthlyQuotaBanner` from Techie (not appropriate for kids)
- Fixed responsive layout: `h-screen + overflow-hidden` prevents header/footer overlap
- Fixed `MathLabModal` positioning (absolute instead of fixed) and pointer events (unique SVG ID)
- Fixed `logger.error` signature mismatch in geminiService
- Deleted `northamerica-south1` Cloud Run service (consolidating to us-central1 only)
- Cloud Build trigger is active and connected to GitHub repo (push to main auto-deploys)

#### State of Work
- Latest commit: `844a815` (deployed, live on us-central1)
- Build verified: Vite build passes cleanly
- Health check confirmed current: timestamp matches real-time

### Session: 2026-05-07

#### Technical Decisions
- **Total Architectural Cleanse**: Removed all client-side Gemini SDK (`@google/genai`) imports and usage from Techie.
- **Proxy Enforcement**: Eliminated `getAI` and `customKey` parameters from `geminiService.ts`. All calls now route via `/api/techie` backend proxy.
- **UI Unification**: Replaced "Cosas Pro" legacy label with "Techie Tools".
- **Standardized Model Selection**: Integrated `ModelSelector` directly into `ChatInput` for Techie, removing it from the top of the chat for a cleaner look.
- **Asset Stabilization**: Generated and implemented local `/techie-mascot.png` to fix broken 404 image link.

#### Contextual Shortcuts
- Techie Proxy: `fetch('/api/techie', ...)`
- Mascot Path: `/techie-mascot.png`
- Model Selection State: Managed in `TechieMain.tsx`, passed to `ChatInput.tsx`.

#### State of Work
- Techie is now 100% compliant with the "Immortal Architecture" (proxy-only, no client keys).
- Mascot is restored locally.
- Engine selection is now standardized with Corporate GPT.

#### Next Steps
1. Smoke test image generation in Techie to ensure it also follows the proxy route.
2. Monitor server logs for any model-routing errors (OpenRouter).
3. Final review of CSS scoping to ensure `ModelSelector` doesn't break Techie's kid-friendly aesthetic.

